# Dieser Ordner enthält Schlüssel und gespeicherte Nachrichten.
# Über den Browser ist hier nichts zu holen.
Require all denied

# Ältere Apache-Versionen (2.2)
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
